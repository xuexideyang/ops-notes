# 故障排查

## 1. CPU 飙升
**现象**：`top` 看到某个进程 CPU 使用率接近 100%
**排查**：
- `top -o %CPU` 找到 PID
- `ps aux | grep PID` 确认进程
- `kill -9 PID` 终止异常进程
**预防**：生产环境避免死循环脚本，用 `crontab` 限制执行时间

## 2. 磁盘空间满
**现象**：写入文件报错 `No space left on device`
**排查**：
- `df -h` 看哪个分区满了
- `du -sh * | sort -rh | head -10` 找大文件
- `lsof | grep deleted` 找已删除但未释放的文件
**解决**：删除无用文件，或重启持有已删除文件的进程

## 3. 端口被占用
**现象**：启动服务报 `Address already in use`
**排查**：
- `ss -tlnp | grep 端口号` 看谁占用了
- 如果是容器：`docker ps` 找对应容器，`docker stop` 或 `docker rm -f`
- 如果是系统进程：`kill -9 PID`

## 4. Docker 容器起不来
**常见原因**：
- 端口冲突 → `ss -tlnp` 检查
- 名称冲突 → `docker ps -a` 找到旧容器，`docker rm -f`
- 镜像不存在 → `docker pull` 拉取
- 内存/CPU 不足 → `docker stats` 查看资源
- 挂载卷权限问题 → 检查宿主机目录权限

## 5. MySQL 连接数过多
**排查**：
- `SHOW PROCESSLIST;` 看当前连接
- `SHOW VARIABLES LIKE 'max_connections';` 看最大连接数
- 调整 `max_connections` 或优化慢查询


## 6. Nginx 502 Bad Gateway

**现象**：访问网站返回 502 Bad Gateway

**排查步骤**：
1. 查看 Nginx error.log：`tail -50 /var/log/nginx/error.log`
   - `Connection refused` → 后端服务未启动或端口不对
   - `Connection timed out` → 后端响应超时或防火墙拦截
   - `no live upstreams` → 所有后端节点都挂了
2. 检查后端进程：`ps aux | grep 后端服务名`
3. 检查后端端口：`ss -tlnp | grep 端口号`
4. 绕过 Nginx 直接测试后端：`curl -v http://127.0.0.1:后端端口`
5. 检查防火墙和 Nginx 配置文件

**常见原因**：
- 后端服务未启动
- 端口被占用
- 防火墙拦截
- `proxy_pass` 配置错误


## 7. 磁盘空间被占用但找不到大文件

**现象**：`df -h` 显示分区满了，但 `du -sh *` 找不到大文件

**排查步骤**：
1. 查找已删除未释放的文件：`lsof | grep deleted`
2. 找到对应进程 PID，重启或 kill 该进程释放空间
3. 如果进程不能停，用 `cat /dev/null > /path/to/deleted_file` 清空文件内容

**常见原因**：进程打开了文件，文件被 `rm` 删除了，但进程未释放句柄


## 8. load average 高但 CPU 使用率低

**现象**：`load average` > 10，但 `top` 里 CPU 使用率不到 20%，`%wa` 也不高

**排查步骤**：
1. `free -h` 看 `available` 是否接近 0
2. `vmstat 1` 看 `si/so`（Swap）和 `cs`（上下文切换）
   - `si/so` 持续 > 0 → 内存不足，频繁使用 Swap
   - `cs` 很高 → 锁竞争或线程争抢

**解决方案**：
- 内存不足 → 扩容内存或优化应用
- 上下文切换高 → 排查锁竞争或线程池配置


## 9. MySQL 主从复制中断

**现象**：`Slave_IO_Running: No` 或 `Slave_SQL_Running: No`

**排查步骤**：
1. `SHOW SLAVE STATUS\G` 查看状态
2. 看 `Last_IO_Error` 或 `Last_SQL_Error` 字段

**常见原因及处理**：
| 报错 | 原因 | 处理方式 |
|------|------|----------|
| `Connection refused` / `Can't connect` | 网络/防火墙/端口问题 | 修好后 `STOP SLAVE; START SLAVE;` |
| `Access denied` | 复制账号密码错误 | 修好后 `STOP SLAVE; START SLAVE;` |
| `could not read event` / `file not found` | 主库 binlog 被清理 | `RESET SLAVE ALL;` 重新搭建主从 |


## 10. Zabbix Agent 连不上（ZBX 红色）

**现象**：Zabbix Web 显示主机 ZBX 图标红色，但 `ping` 能通

**排查步骤**：
1. `ps aux | grep zabbix_agentd` 确认进程是否存活
2. `ss -tlnp | grep 10050` 确认端口是否监听
3. `zabbix_get -s 目标IP -k system.hostname` 从 Server 端测试连通性
4. 检查防火墙和安全组是否放行 10050 端口
5. 检查 Agent 配置文件：`Server` 和 `Hostname` 是否与 Web 端一致


## 11. Docker 容器启动后立即退出

**现象**：`docker run` 后容器立刻退出

**排查步骤**：
1. `docker ps -a` 查看退出码
   - `Exited (0)` → 正常退出（不是故障）
   - `Exited (非0)` → 异常退出
2. `docker logs 容器名 --tail 50` 查看日志
3. `docker inspect 容器名 | grep -A 5 "Cmd"` 检查启动命令

**常见原因**：`CMD` 写成了后台启动（如 `service nginx start`），应改为前台启动（如 `nginx -g "daemon off;"`）


## 12. Nginx 端口被占用（Address already in use）

**现象**：启动 Nginx 报错 `bind() to 0.0.0.0:80 failed (98: Address already in use)`

**排查步骤**：
1. `ss -tlnp | grep :80` 看谁占了 80 端口
2. 如果是已有 Nginx 进程 → `systemctl stop nginx` 或 `kill`
3. 如果是容器占用 → `docker stop 容器名` 或换端口
4. 如果无法停 → 修改 Nginx 配置 `listen` 端口，`nginx -t` 检查后重载


## 13. Docker 容器日志过大

**现象**：`docker system df` 显示磁盘占用高，容器日志文件很大

**排查**：
```bash
docker logs 容器名 --tail 100        # 看最近日志
docker inspect 容器名 | grep LogPath  # 查看日志文件位置
ls -lh $(docker inspect 容器名 | grep LogPath | awk -F'"' '{print $4}')